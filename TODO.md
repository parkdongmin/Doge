# 할 일

## 남은 할 일 요약 (2026-09-03 갱신)

**미푸시 커밋 2개:** `3427fa3`(다기기 동기화 안내 문구 + 9/2 테스트 라운드 반영) ·
`c417e56`(폭락 행성 매도가 마이너스 버그 + 매도/강화 UI + ⓘ 설명문 톤 통일). 그 앞
8/31 커밋 5개는 `origin/main` 반영됨.

### 2026-09-03 완료 (커밋 `c417e56`)
- [x] 폭락 행성 매도가가 마이너스로 가던 버그 (`--46 코인`) — marketAdjustment 바닥 clamp
- [x] 매도 다이얼로그 정리 (투자액 / 시세 변동 % 병기 / 수수료 0줄 숨김)
- [x] 투자 현황 카드: 투자액 / 현재 매도가 (중복 줄 제거)
- [x] 강화 다이얼로그 효과 미리보기 ("분당 생산량 N → M")
- [x] 분당 드롭 자원 폰트 통일
- [x] ⓘ 설명문 전체 톤 통일 (해요체) — 자세히는 아래 해당 섹션

### 실기기 테스트
- [x] 튜토리얼 — Part 1 / Part 2 / 정거장·행성상세 원샷 버블. 2026-08-31 확인
- [x] 스플래시·로그인 새 디자인 — 유저 확인 완료, 현재 상태 유지 (2026-09-02)
- [x] 클라우드 복원 기존 유저 — 튜토리얼 전부 안 뜸 확인 (2026-09-02)
- [x] 스냅샷 마이그레이션 회귀 — 앱 삭제→재설치→로그인 복원으로 확인됨. 코드로도
      `ignoreUnknownKeys`+기본값 경로 + `SnapshotLoaderTest` 6개 검증. 실질 가장 오래된
      세이브가 8/27이라 모양 차이 거의 없어 위험 낮음 (2026-09-02)
- [x] `users` 필드 축소 — 코드 점검: `saveUser()` 쓰는 필드 = `LeaderboardRepositoryImpl`
      읽는 필드 일치, 삭제된 email/photoUrl/fcmToken 읽는 죽은 코드 없음. 다기기 로그인
      정상 (2026-09-02)
- [x] 클라우드 세이브 다기기 LWW — Part A(순차 동기화, 에뮬↔실기기) 정상 (2026-09-02).
      Part B(동시 오프라인 충돌 = 먼저 재동기화한 기기가 이기고 나머지 세션 유실)는
      의도된 동작이라 스킵
- [x] 비행기모드 스플래시 — pull 실패해도 안 멈추고 로컬 진입, 온라인 복귀 후 복원 정상 (2026-09-02)
- [x] 행성 이벤트 — 소식 로그·상세화면 표시·백그라운드 알림·`doge://planet` 딥링크 전부
      정상 (2026-09-02, 임시 오버라이드로 확인 후 원복). 밤샘 자연발생 확인은 불필요
- [x] 손실 다이얼로그 — 실제로 뜨고 코인도 바닥 클램프로 차감됨 확인 (2026-09-02)

### 바로 할 만한 것
- [x] 다기기 동기화 안내 문구 — 로그인 화면(상시) + 스플래시 "동기화 중" 아래 두 곳에
      추가 (`LoginScreen.kt`/`SplashScreen.kt`, `BodyReading`). 2026-09-02
- [x] 튜토리얼 갈래 2 나머지 ⓘ — `InfoDialog`가 각 화면 ⓘ로 이미 커버하므로 중복이라 우선순위
      없음으로 유지하기로 함(2026-09-02). 연구소 ⓘ는 필요 없다고 확정(2026-09-03) — 종료

### 클라우드 세이브 Phase 4 (나중)
- [x] 충돌 다이얼로그 — 조용한 LWW로 충분하다고 판단, 구현 안 하기로 결정 (2026-09-03)
- [x] 로그아웃 시 push 후 세션 정리 (2026-09-03) — `CloudSaveManager.signOutAndClearLocal()`
      (push → `LocalSnapshotDataSource.clearLocal()`로 Room 전체 초기화 → `CloudSyncPrefs.resetSyncState()`
      로 rev 추적 리셋(deviceId는 유지) → Firebase signOut). `AuthViewModel.signOut()`이 이걸 호출.
      버튼 위치는 정거장에 뒀다가 "정거장에서 로그아웃은 뜬금없다"는 피드백으로 자산 탭
      헤더로 이동, 스타일도 맨 텍스트 → 판매 버튼과 같은 아웃라인 칩(중립 톤)으로,
      위치도 제목 옆(위쪽 붕 뜸) → 제목+설명문 Column 오른쪽에 `Alignment.Bottom`으로
      설명문 아랫줄과 맞춤 — 유저 확인 완료(2026-09-03). `AssetScreen`에 확인
      다이얼로그+로딩 오버레이도 있음. `MainScreen`/`MainActivity`에 `onSignOut` 콜백 배선 →
      로그인 화면으로 이동. `compileDebugKotlin` 통과, 실기기 확인 완료(2026-09-03)
- [x] 죽은 컬럼 `Planet.currentValue` 제거 (2026-09-03) — `Planet`/`PlanetEntity`/`PlanetMapper`/
      `GeneratePlanetsUseCase`/`ExploreViewModel`/`ExplorationStore`에서 필드 제거,
      `MIGRATION_17_18`(DB v17→v18, `planet_table` 재생성)로 컬럼 드롭. `compileDebugKotlin` 통과

### 자잘한 검토
- [x] `OFFLINE_PROFIT_DIALOG_THRESHOLD_MINUTES`(10분) 분리 여부 — 그대로 공유 상수 유지로 결정 (2026-09-03)
- [x] `.kotlin/` 빌드 산출물 디렉터리 `.gitignore` 추가 (2026-09-03, 추적 안 되고 있었음 — 추가)
- [x] 미푸시 커밋 2개 push (2026-09-03, `3427fa3`/`c417e56` 포함 `b9fad21`까지 push 완료)

### 출시 전 (기능·디자인 마무리 후, 아래 "출시 전 확인 필요" 섹션)
- [ ] Cloud Functions 배포 여부 콘솔 확인
- [ ] Play 출시 서명(keystore + `signingConfigs`)
- [ ] release `isMinifyEnabled`(R8) 결정
- [ ] 사운드 에셋 확보·적용

---

## 다음에 할 일: 튜토리얼 + 설명 부족 보완

2026-08-24, 행성 시세/이벤트 시스템 작업 중 나온 다음 우선순위. 게임 자체에 설명이 너무
없다는 문제를 두 갈래로 나눠서 접근:

### 갈래 1 — 첫 진입 튜토리얼 (2026-08-31 구현 + 실기기 확인 완료, 커밋 `cea7340`+`2f8a881`)

**전달 방식: 대화 오버레이 + 타깃 하이라이트.** 별도 튜토리얼 화면 안 만듦.
화면 하단에 NPC(관제탑 AI) 대사 버블(네임플레이트 + 텍스트 + CTA), 필요한 순간에만
실제 버튼/영역을 스포트라이트(전체화면 스크림 + 4밴드 컷아웃)로 밝힘.

- **NPC = 관제탑 AI.** 이름 없는 시스템 음성 톤. 기존 외계 문명 스토리(`StoryContent`)와
  서사 안 겹침. 초상 에셋 없음 — GoldAccent 네임플레이트만.
- **`StoryEvent` UI 재사용 안 함** — `expeditionId` 필수 + 선택지가 `(ResourceType, 수량)`
  보상 쌍 고정이라 자유 텍스트 대사에 안 맞음. 가벼운 대화 오버레이를 새로 만듦.
- **스코프 = 최소 (탐사 1회 파견 + 방치수익 이해까지).** 강화·매도는 맥락형 원샷 버블로 분리.
- **스타터 자산 지급:** 신규 플레이어는 코인 10,000만 있고 우주선·우주인·행성 0.
  `SeedStarterAssetsUseCase`가 신규 게임 첫 진입 시 **정찰선 MK-1 1대 + 인턴 대원
  ("박 탐석", MINERAL, 숙련도 20) 1명 + 스타터 행성 1개**(무대기 행성,
  `GeneratePlanetsUseCase.starterPlanet()` — 최저 스탯 결정론)를 무상 지급.
  - 스타터 행성을 넣은 이유: Part 2(방치수익 = 핵심 훅)가 "행성 획득"에 걸려 있는데
    실제 발견 확률이 18%라, 신규 유저가 탐사를 5~10번 돌릴 때까지 방치수익을 한 번도
    못 보고 이탈할 위험 → 아이들 게임의 "첫 생산기 무료" 패턴. Part 2가 Part 1 직후 바로 뜸.
  - `UserRepository.initialize()`가 `Boolean`(신규 여부) 반환하도록 바꾸고
    `ExploreViewModel.init`에서 true일 때만 시드 + `TutorialPrefs.startedFresh = true`.
- **게이팅:** 모든 단계에 `startedFresh`(이 기기에서 완전 신규 시작) 선행 조건 →
  클라우드 복원 기존 유저는 튜토리얼 자체가 안 뜸.
  - Part 1: `startedFresh && !part1Done && 진행중 탐사 0` · 탐사 탭에서만
  - Part 2: `startedFresh && part1Done && !part2Done && 보유행성 ≥ 1` · 행성 탭에서만
  - 맥락형 원샷: `startedFresh && part1Done && !xIntroDone` · 해당 라우트 첫 방문
- **저장:** `TutorialPrefs`(SharedPreferences) — `startedFresh`/`part1Done`/`part2Done`/
  `hqIntroDone`/`upgradeIntroDone`. 기기 로컬, 클라우드 세이브에 안 실림.

**구현 조각 (2026-08-31, `compileDebugKotlin` 통과, 실기기 테스트 전):**
- [x] `SeedStarterAssetsUseCase` (정찰선+대원+행성), `UserRepository.initialize(): Boolean`
- [x] `TutorialPrefs.kt`
- [x] `presentation/tutorial/TutorialTarget.kt` — `TutorialTargetRegistry`
      (`mutableStateMapOf<TutorialTarget, Rect>`), `LocalTutorialTargets` CompositionLocal,
      `Modifier.tutorialTarget()` = `onGloballyPositioned { boundsInWindow() }`
- [x] `TutorialViewModel.kt` — 진행중 탐사·보유 행성·라우트·prefs를 `combine`해
      `TutorialStep`(None/Part1(page)/Part2/HqIntro/UpgradeIntro) 계산. `onRouteChanged`
- [x] `TutorialOverlay.kt` — 스크림 + 4밴드 컷아웃(BlendMode 없이) + GoldAccent 강조
      테두리 + 관제탑 대사 버블. 아무 곳이나 탭 = 진행. 타깃이 화면 아래쪽이면 버블 위로 앵커
- [x] Part 1 (intro → `EXPLORE_FIRST_CATEGORY` 하이라이트), Part 2 (방치수익)
- [x] 맥락형 원샷 버블 2개 — 정거장(`hq`) 첫 방문 / 행성 상세(`planet_detail/{planetId}`)
      첫 진입. Hq: "대원·우주선·연구". Upgrade: "강화로 생산량↑, Lv.11+ 위험, 매도로 정리"
- [x] **실기기 확인 (2026-08-31)**: Part 1 / Part 2 / 정거장·행성상세 원샷 버블 정상.
      로그인 직후 안 뜨고 탭 전환 후에야 뜨던 버그는 `TutorialPrefs`를 StateFlow로 바꿔
      수정(`2f8a881`). `StartExpeditionUseCase` 15초 TEST 오버라이드는 커밋 전 원복됨.
      스타터 정찰선/대원/행성도 정상 지급 확인.
      - [ ] 클라우드 복원 유저(기존 세이브 있음)한테는 튜토리얼 전부 안 뜨는지 — 아직 미확인

### 갈래 2 — 게임 곳곳 설명 부족 보완 (화면마다 조금씩)

`PlanetDetailScreen`의 `StatsInfoDialog`(ⓘ → 용어 설명 팝업) 패턴을 다른 화면에 복제.
공용 껍데기 `presentation/component/InfoDialog.kt` (`InfoDialog`/`InfoEntry`, 폭은
`fillMaxWidth(0.88f)` — 폴더블 대응, 설명문은 `BodyReading`/Pretendard).

- [x] **탐사 정보 ⓘ** (`ExpeditionInfoDialog`, ExploreScreen "탐험 종류 선택" 옆) —
      카테고리별 드랍 자원 + 해금 레벨 + "행성은 어느 탐사에서든 발견(행성 탐사 +5%p)"
      + 자원 용도. 신규 유저가 광물 탐사만 반복하는 문제 대응
- [x] **우주선·우주인 효과 ⓘ — 3곳** — 팀 빌더(`TeamInfoDialog`) / 격납고 앱바
      (`ShipInfoContent`) / 우주인 센터 앱바 (`CrewInfoContent(showGrades=true)` — 전문분야·
      숙련도·인원 효과 + 인턴~레전드 등급표). 값 출처: `CompleteExpeditionUseCase` /
      `StartExpeditionUseCase` / `AstronautGrade`
- [x] **자산 탭 "보유 자산" ⓘ** — DGT/USDT/행성 NFT 정의 + 총 자산(DGT) + 랭킹 기준
- [x] 자산 탭 정리: USDT 스케일을 총 자산과 일치(`/1000` 제거), 요약 스탯에서 중복된
      "행성 시세 합계" → "분당 순생산"으로 교체, "NFT 자산" → "행성 NFT"
- [x] 설명문 폰트를 픽셀(`bodySmall`) → `BodyReading`(Pretendard)로 — 새 ⓘ 모달·튜토리얼
      버블. 짧은 항목명·제목·버튼은 픽셀 유지. `Type.kt`가 이미 분리해둔 스타일
- [x] UI 문구 "본부" → "정거장" 통일 (`StoryContent`의 "본부에 보고"는 서사상 다른 뜻이라 유지)
- [ ] 나머지 화면 ⓘ (연구소 분야별 효과, 강화 성공률 표, 자원별 용도 등) — 우선순위 미정

## 스플래시·로그인 화면 리디자인 (2026-08-31, 커밋 `5253431`)

인게임 화면들은 다 어두운데 스플래시가 플랫 검정 단색이라 밋밋하고 싸 보인다는 데서 시작.

- **배경 `BrandBackgroundGradient`** (`theme/Color.kt`) — 상단 `#0A1225` 유지하다 하단
  1/3에서만 `#24448F`(SpaceBlue)로. 스플래시·로그인 공통, 한 곳에서 관리.
- **하단 행성 일러스트** `splash_planet.png` (에셋은 유저 제작·교체). `fillMaxWidth`,
  `BottomCenter`. 로고 블록은 weight 0.5/0.68 스페이서로 상단쪽.
- **로고**: "DOGE" → "Doge"(앞글자만 대문자), 46sp로 키움. 스플래시·로그인 동일.
- **로그인 화면도 같은 배경·행성·로고 위치** → 스플래시→로그인 전환이 이어지는 느낌
  (로딩 점 자리에 Google 로그인 버튼).
- `SplashScreen`의 시각 부분을 `SplashContent(isSyncing)`로 분리(프리뷰용, 지금은 프리뷰 제거).
- [x] 실기기 확인 완료 (2026-09-02) — 현재 디자인 유지. 다기기 동기화 안내 문구를
      로그인 화면(상시)·스플래시 "동기화 중" 아래에 추가

## 클라우드 세이브 (Firestore) — Phase 1~3 구현 완료 + push/복원 검증됨

2026-08-27. 설계·단계는 `~/.claude/plans/tidy-whistling-pond.md` 참고.
게임 상태 전체(Room 12개 테이블, `planet_event_log` 제외)를 `GameSnapshot`으로 직렬화해
`saves/{uid}` Firestore 문서 1개에 JSON으로 백업. push 트리거 = 앱 백그라운드(ON_STOP) +
30분 주기 루프. 스플래시에서 1회 pull+복원. 충돌 = 조용한 LWW(`rev` 비교).

**검증됨:** 홈버튼 → `saves/{uid}` 문서 생성 확인, 앱 삭제→재설치→로그인 복원 확인.
**2026-09-02 추가 검증 완료:** 비행기모드 스플래시(안 멈춤), 다기기 LWW Part A(순차 동기화
에뮬↔실기기), `users` 필드 축소(코드 점검 — 쓰는 필드=읽는 필드 일치), 스냅샷 마이그레이션
회귀(재설치 복원 + 유닛테스트). 남은 것: 리더보드 상위권 회귀(계정 여러 개 필요), LWW Part B
(의도된 동작이라 스킵).

**Firestore 규칙 (2026-08-31 레포 편입 `8340c8d` + 콘솔 배포 완료):** saves는 본인만
read+write, users는 read 허용(리더보드)·write 본인만, notifications/pending은 create만,
나머지 catch-all은 `if false`로 완전 차단. `users` 문서에서 `email`·`photoUrl`·`fcmToken`
제거 → 리더보드 read 허용 때문에 다른 유저에게 읽혀도 무방한 데이터만 남음(별도 컬렉션 분리 불필요).

**스냅샷 스키마 마이그레이션 (2026-08-31 완료, `d788a6f`):** 정확히-일치 `roomDbVersion`
게이트 제거 → `SnapshotLoader.loadSnapshot()` (파싱 → `SnapshotMigrations` → 역직렬화).
필드 추가/삭제는 `ignoreUnknownKeys`+기본값이 처리, 이름/타입/구조 변경만 step 등록.
`Loaded` / `Corrupt`(=FAILED) / `Incompatible`(미래 버전·경로 없음, =SKIPPED_VERSION).
유닛테스트 `SnapshotLoaderTest` 6개. 이제 Room 스키마 자유롭게 변경 가능.

**Phase 4 (나중):**
- ~~충돌 다이얼로그~~ — 조용한 LWW로 충분하다고 판단, 구현 안 하기로 결정 (2026-09-03).
  실제 충돌은 동시 오프라인 다기기 플레이라는 드문 케이스에만 생기고, 그때 한 기기 세션이
  사라지는 것은 감수하기로 함
- ~~로그아웃 시 push 후 세션 정리~~ — 완료 (2026-09-03, `CloudSaveManager.signOutAndClearLocal()`
  + `HQScreen` 로그아웃 버튼)
- ~~죽은 컬럼 `Planet.currentValue` 제거~~ — 완료 (2026-09-03, `MIGRATION_17_18`)

## 행성 시세/이벤트 시스템 (1차 구현 완료, 2026-08-27 커밋됨)

**지금 상태**: 테스트 오버라이드 전부 원래 값으로 되돌리고 커밋함 (클라우드 세이브 작업이
이 시스템의 DB 스키마 v17에 의존해서, 먼저 커밋해야 했음).

- `GameConstants.planetEventIntervalHours` = `6.0 - (risk - 2) * 0.0306` (risk=2→6h,
  risk=100→3h) **최종 확정값**
- `PLANET_EVENT_NOTIFY_DELTA_THRESHOLD` = `0.25`, `PLANET_EVENT_DELTA_MIN/MAX` = `0.03`/`0.30`,
  `RollPlanetEventUseCase.isBad` = `Random.nextInt(100) < planet.eventRate` — 전부 실값으로 복귀

**2026-09-02 실기기 확인 완료** — 임시 오버라이드(간격 0.01h, 알림 임계 0.05)로 소식 로그·
상세화면 "생산 진행/중단"·백그라운드 워커 알림·`doge://planet` 딥링크 전부 정상 확인 후
오버라이드 원복. 밤샘 자연발생 확인은 불필요하다고 판단.

### 이벤트 간격 재조정 + 큰 폭 이벤트 알림 (2026-08-24, 구현 완료)
간격 공식을 두 단계에 걸쳐 단축. 원래 뜸하게 잡은 이유가 "너무 잦으면 알림 스팸처럼
느껴진다"였는데, 지금 행성 이벤트는 알림 자체가 없고(포그라운드 정산 시점에만 조용히
롤되고 "소식" 탭 들어가야 보임) 그 전제가 애초에 적용 안 된다는 걸 깨달음:
1. `42.0 - (risk-2)*0.3265`(risk=2→42h, risk=100→10h, 하루 1~2회 수준) →
   `14.0 - (risk-2)*0.1122`(risk=2→14h, risk=100→3h)
2. 실기기로 체감해보니 그마저도 안전한 타입(risk 낮은 쪽)은 여전히 뜸하게 느껴져서, 최고점만
   한 번 더 낮춤 → **`6.0 - (risk-2)*0.0306`(risk=2→6h, risk=100→3h) — 최종 확정, 테스트용
   아님**

타입별 실제 간격(현재 risk 범위 기준):

| 타입 | risk 범위 | 이벤트 간격 |
|---|---|---|
| 블랙홀 | 80~100 | 3.0~3.6h |
| 용암 | 40~60 | 4.2~4.8h |
| 가스자이언트I | 20~40 | 4.8~5.4h |
| 사막 | 15~30 | 5.1~5.6h |
| 빙하 | 15~25 | 5.3~5.6h |
| 군도·가스자이언트II | 10~20 | 5.4~5.8h |
| 항성 | 8~20 | 5.4~5.8h |
| 습지·은하 | 5~15 | 5.6~5.9h |
| 무대기·소행성 | 2~10 | 5.8~6.0h |

`GameConstants.PLANET_EVENT_NOTIFY_DELTA_THRESHOLD`(알림 기준)는 `0.25`로 되돌림 (2026-08-27).

**주의: 이 간격 상수는 "이벤트가 실제로 굴러가는" 주기일 뿐, 알림이 오는 주기가 아님.**
실기기 테스트 중 헷갈렸던 부분 — 이벤트 롤 자체(DB 반영, 소식 로그)는 앱을 켜놓으면 이
간격대로 계속 일어나지만, 알림을 보내는 `PlanetEventWorker`는 완전히 별개로 **1시간
주기로만** 깨어나서 그 시점에 밀린 이벤트를 확인·알림 발송함(Android 자체가 백그라운드
주기 작업을 15분보다 짧게 허용 안 하기도 함). 그래서 간격을 아무리 짧게 잡아도 알림은
최대 1시간에 한 번만 옴 — 다음에 "알림이 왜 이 간격대로 안 오지" 질문이 다시 나오면 이
문단부터 확인.

이어서 "큰 폭으로 움직인 이벤트만 알림 보내기"(주식 앱의 급등락 알림과 같은 개념) 구현:
- 이벤트 롤 로직(`CollectProfitUseCase`에 박혀있던 private 함수)을 `RollPlanetEventUseCase`로
  분리 — 포그라운드 수집 경로와 새 백그라운드 워커가 같은 로직·같은 `lastEventTime` 가드를
  공유해서 이중 롤 걱정 없이 재사용
- 새 워커 `PlanetEventWorker` 추가(1시간 주기, `PlanetMaintenanceWorker`와 같은 패턴) —
  보유 행성을 순회하며 밀린 이벤트를 롤하고, 그중 등락폭(델타)이
  `GameConstants.PLANET_EVENT_NOTIFY_DELTA_THRESHOLD`(0.25, 델타 범위 3~30%p 중 상위
  ~18.5%) 이상인 것만 모아 알림 발송. 알림 없는 나머지 이벤트는 조용히 DB·소식 로그에만 반영
  (기존 동작 그대로). 델타 범위는 원래 5~30%p였다가 2026-08-24에 최솟값만 3%p로 한 번 더
  낮춤(`GameConstants.PLANET_EVENT_DELTA_MIN`, 테스트용 아니고 최종 확정)
- 여러 행성이 동시에 큰 폭으로 움직이면 "폭등 N · 폭락 M" 식으로 묶어서 한 알림으로 발송
  (탐사 완료 워커의 개수 묶음 패턴과 동일)
- 새 알림 채널 `channel_planet_event`("행성 시황 속보", IMPORTANCE_HIGH) 추가
- 행성명에 "이/가" 조사를 종성 유무에 맞게 자동으로 붙이는 유틸(`util/KoreanText.kt`,
  `String.withSubjectParticle()`) 신설 — "블랙홀이 폭락했습니다" / "은하가 폭등했습니다" 등
  동적 문구에 정확한 조사가 붙게 함
- `./gradlew compileDebugKotlin` 통과 확인.
- **실기기 확인 중간 결과**: 처음엔 알림이 하나도 안 와서 점검했는데, 원인은 코드 버그가
  아니라 (1) 앱 알림 권한 자체가 꺼져있었음(`dumpsys notification`의 `allowNoti=false`로
  확인, 설정에서 켜서 해결) (2) 새로 등록된 주기 워커라 첫 자연 실행까지 최대 1시간이 걸림
  (착각하고 "새 워커라 곧바로 실행될 것"이라 안내했던 게 틀렸음 — `PeriodicWorkRequest`는
  최초 실행도 지정 간격만큼 기다린 뒤 시작함). `adb shell cmd jobscheduler run -f
  com.doge.simulator <job id>`로 강제 실행해서 워커가 크래시 없이 정상 완료되는 것까지는
  확인(`WM-WorkerWrapper: Worker result SUCCESS ... PlanetEventWorker`)
- **2026-09-02 최종 확인 완료**: 알림 실제로 화면에 뜨는 것 + 알림 탭 시 `doge://planet`
  딥링크로 행성 탭 이동까지 유저 육안 확인.

### 왜 이 논의를 시작했나
연출 작업을 마치고 실기기 테스트까지 끝낸 뒤, "지금 이대로 재밌나?"라는 질문에서 시작됨.
게임 루프(탐사 → 발견 → 방치수익 → 강화 → 재편성)가 방치형치고 얕은 설계는 아니지만,
**모든 수치가 내 행동에만 반응하고, 외부에서 움직이는 변수가 없어서 "지금 안 들어가면
손해 본다"는 압박이 구조적으로 없음**이 핵심 문제로 진단됨. 과거에 만들었던 "내 이름은
주식왕"(임의 주가가 유저 매수/매도로 실시간 변동, 손익을 겨루는 게임)과 비교하면서 나온 통찰 —
그 게임의 재미는 "숫자가 오른다"가 아니라 "숫자가 (내가 없어도) 움직인다"에서 왔다는 것.
지금은 가만히 있으면 무조건 이득 보는 구조라, 이걸 깨는 게 목표.

### 설계가 세션 중 크게 바뀜 (아래가 최종안, 중간 과정은 "폐기된 안" 참고)
처음엔 "위험도 게이지 + 관리(낮추기) 액션" 3축(강화/관리/매도) 구조로 시작했다가,
토론 중 "관리로 위험도를 언제든 리셋할 수 있으면 매도가 무의미해진다"는 문제가 발견되어
관리 액션 자체를 없애고 강화/매도 2축 + 정적 이벤트 확률 구조로 전면 수정됨. 아래 "최종
확정 설계"가 지금 기준이고, 관리 액션·위험도 게이지 관련 내용은 전부 폐기됨.

### 최종 확정 설계
- **두 가지 행동 축**: 강화(공격 — 내가 통제하는 리스크, 버튼 누르는 즉시 확정되는 도박,
  기존 그대로 유지) / 매도(현금화 겸 손절·익절 — 기존 그대로 유지). "관리" 액션은 만들지
  않기로 최종 결정.
- **위험도 게이지 없음 — 기존 정적 스탯 `Planet.risk`/`eventRate`를 그대로 재활용**.
  원래 있었지만 죽어있던 필드(생성 시 타입별 범위에서 한 번 롤되고 그 뒤로 안 바뀜,
  `GeneratePlanetsUseCase.kt`/`PlanetMetaDataTable.kt`)를 원래 의도대로 사용:
  - `risk` = 이 행성에 이벤트가 얼마나 "자주" 터지는지(빈도) — 기존 타입별 범위 그대로 유지
  - `eventRate` 필드는 더 이상 타입별 값을 안 쓰고, **희귀도(rarity) 기준 확률표로 대체**
    (아래 "좋음/나쁨 확률" 참고) — 이유는 아래 폐기된 중간안 참고
- **이벤트 발생 주기**: 시간 기반, 타임스탬프(`lastEventTime`, 신규 필드)로 계산(방치수익과
  동일 패턴 — 앱 안 켜도 계산되고 백그라운드 틱 불필요). `risk`가 높을수록 평균 간격이 짧음.
  "너무 뜸하면 체감이 안 되고, 너무 잦으면 스팸처럼 느껴진다"는 기준으로 최종 조정:
  - 가장 위험한 타입(블랙홀, risk 80~100) → 평균 **10.0~16.5시간** 간격 (하루 1~2회)
  - 가장 안전한 타입(risk 2~10, 무대기·소행성) → 평균 **39.4~42.0시간** 간격 (1.6~1.75일)
  - 그 사이 타입들은 대략 1.2~1.75일 사이에 고르게 분포(용암 23~30h, 사막 33~38h,
    빙하 35~38h, 군도·가스자이언트II 36~39h, 항성 36~40h, 습지·은하 38~41h)
  - **최종 공식**: `이벤트 평균 간격(시간) = 42 − (risk − 2) × 0.3265`, risk값에 선형 보간
    (risk=2 → 42시간, risk=100 → 10시간)
  - **체크 방식**: 방치수익 수집 시점(포그라운드 진입/수집 시)에 "마지막 이벤트 이후 이
    간격을 넘었으면 딱 1번만 롤". 며칠을 방치했어도 몰아서 여러 번 굴리지 않고, 확인하는
    순간 결과 하나만 나옴 — 캐치업 로직 불필요, "돌아와보니 뭔가 하나 일어나 있었다"는
    느낌에도 더 맞음. 보유 행성 전체를 순회하며 체크해도(방치수익 계산과 동일한 O(n)
    루프) 연산량이 미미해 슬롯이 아무리 늘어도 성능 문제 없음
- **좋음/나쁨 확률 — 희귀도(rarity) 기준, `risk`(빈도)와 완전히 분리**: 처음엔 `eventRate`를
  타입별로 다르게 굴려 그대로 쓰려 했으나, "위험도가 높을수록(=비싸고 희귀한 행성일수록)
  나쁜 확률도 높다"는 건 고위험·고수익이 아니라 그냥 "비싼데 계속 손해보는 나쁜 투자처"가
  되어버리는 역전 문제였음(폐기 사유는 아래 참고). **비싸고 희귀할수록 확률은 유리하게,
  단 최상위 등급도 완전 무결점은 아니게(천장 있음)** 재설계:

  | 희귀도 | 나쁜 이벤트 확률 | 좋은 이벤트 확률 |
  |---|---|---|
  | COMMON (습지·사막·군도·무대기) | 45~55% | 45~55% |
  | UNCOMMON (가스자이언트·빙하·용암·소행성) | 40~48% | 52~60% |
  | RARE (블랙홀) | 35~42% | 58~65% |
  | EPIC (은하) | 30~37% | 63~70% |
  | LEGENDARY (항성) | 25~32% | 68~75% |

  `risk`(변동성·빈도)는 타입별로 그대로 다르게 유지 — 블랙홀은 여전히 자주·크게 흔들리는
  스릴 있는 자산이고, 그 흔들림이 평균적으로 얼마나 유리한지는 희귀도가 결정
- **이벤트가 건드리는 것 — 생산 배율 + 시세, 한 번의 롤이 둘 다 같이 결정, 등락폭도 랜덤,
  둘 다 "지속형", 그리고 "덮어쓰기"가 아니라 "누적"** (이 부분은 실기기 테스트하며 재검토돼
  최종안이 바뀜 — 폐기 사유는 아래 "폐기된 중간안" 참고):
  - **생산 배율**: 행성마다 `productionMultiplier`(신규 필드, 기본 1.0) 추가. 이벤트 1회당
    등락폭(델타) `±5%~30%p`가 매번 새로 랜덤 굴려지고, **이전 값에 계속 더해짐(누적)**.
    나쁜 이벤트가 연달아 겹치면 정말로 배율이 0 밑(마이너스, 즉 실제 손해)까지 갈 수 있음 —
    다만 무한정 나빠지거나 좋아지지 않도록 **바닥 −30% / 천장 +200%로 클램프**
    (`GameConstants.PLANET_EVENT_MULTIPLIER_FLOOR/CEILING`). 바닥까지 가려면 최악의
    델타로만 4~5연속 불운해야 해서 "가끔 진짜 사고"라는 긴장감은 유지하면서도, 그냥
    방치만 하는 플레이어에게도 무조건적인 손실이 생길 수 있게 됨(아래 참고).
    `effectiveProduction` 계산에 이 배율을 곱함 — **화면에 표시되는 분당 생산량 숫자
    자체가 바뀌므로 온라인/오프라인 상관없이 항상 실제 수령액과 일치**. `PLANET_PRODUCTION_SCALE`·
    레벨 배율 같은 기존 튜닝된 공식은 안 건드리고 그 위에 곱해지는 배율 하나만 추가
  - **시세**: `marketAdjustment`(신규 필드, 매도가·랭킹에 쓰이는 `buyPrice + upgradeInvestment`
    공식에 항목 하나만 추가) — 매 이벤트마다 따로 누적하지 않고, **"지금 누적된 생산
    배율(위 productionMultiplier)"을 기준으로 매번 다시 계산**: `(생산배율−1.0) × buyPrice
    + (생산배율−1.0) × upgradeInvestment × 0.25`. 이러면 생산 배율과 시세가 서로 다른
    값으로 어긋날 일이 없음(항상 같은 소스에서 파생). 강화를 많이 할수록(=`upgradeInvestment`가
    클수록) 전체 가치 대비 변동폭 비중이 줄어들지만 **완전히 0에 수렴하진 않는 바닥선**이
    있음(0.25배만큼은 항상 남음) — 강화를 아무리 해도 시세 리스크가 완전히 사라지진 않음
  - 둘 다 **"관리"로 되돌릴 방법이 없고, 오직 매도로만 정리 가능** — 나빠진 행성은 손절
    매도, 좋아진 행성은 익절 매도 또는 계속 보유하며 복리로 누적. 이게 드디어 매도를
    실제로 의미 있는 선택지로 만듦
- **"방치만 해도 손해 볼 수 있나?" 검증** — 실기기 테스트 중 나온 핵심 질문: 생산 배율이
  플러스인 한(예: −27%) 누적 수익은 계속 늘어나기만 하지, 안 팔면 "잃는다"는 개념 자체가
  없는 게 아니냐는 지적. 검토 결과 맞는 지적이었고, 위 "누적(클램프)" 방식으로 배율이
  진짜 마이너스까지 갈 수 있게 바뀌면서 해결됨 — 매도 의향이 없는 순수 방치형 플레이어도
  운이 나쁘면 실제로 코인을 잃을 수 있음. "미수금 훔치기"·별도 "심각한 사고 확률 분기" 등
  다른 대안도 검토했으나 이 방식(단일 배율이 자연스럽게 누적되며 마이너스까지 가능)이
  가장 단순하면서 목적을 달성함
- **신규 구매 행성 초기값**: `productionMultiplier` 1.0, `marketAdjustment` 0,
  `lastEventTime` = `acquireTime`
- **UI**:
  - 행성 목록 카드: 배율 상태에 따라 색 점 하나만 표시(좋음=초록/나쁨=빨강/보통=무표시) —
    기존 `StatusGreen`/`StatusRed` 관례 재사용, 별도 배지 시스템 새로 안 만듦
  - 상세화면: 별도 "생산 변동 %" 행을 만드는 대신, **기존 "시간 수익" 행 자체를 부호에 따라
    라벨·색이 바뀌게 함** — 0 이상이면 "생산 진행 +N코인/시"(골드), 0 밑이면 "생산
    중단 −N코인/시"(빨강). 처음엔 "생산 배율" 행을 %로 따로 추가했었는데, 누적치가
    100%를 넘나들 수 있게 되면서 "-115%" 같은 표시가 버그처럼 보인다는 문제가 있어 폐기 —
    퍼센트 대신 코인/시간 단위로 통일해 암산도 없고 이상한 숫자도 안 나오게 함
    ("생산량" 행도 마이너스면 빨강으로 색만 전환). "시세 변동" 행은 원래도 코인 단위라
    그대로 유지
  - "소식" 로그도 같은 이유로 %가 아니라 코인/시간 단위로 통일(`productionDeltaPerHour`) —
    이번 이벤트 하나만으로 바뀐 시간당 코인을 그대로 보여줌(상세화면의 "생산 진행/중단"은
    누적치, 로그는 이번 이벤트 하나의 기여분이라 역할이 다름)
  - 이벤트 발생 자체엔 강화·탐사처럼 강한 연출 안 넣음 — 플레이어가 버튼을 누르는 결정적
    순간이 없는 백그라운드성 이벤트라, 이미 정해둔 "확률 롤의 결정적 순간에만 연출" 원칙에서
    벗어남. 상세화면 들어가서 상태를 확인하는 정도로 충분
  - "다음 이벤트까지 남은 시간" 같은 카운트다운 UI는 안 넣음 (탐사 대기시간 카운트다운도
    같은 이유로 보류했던 것과 동일한 판단 — 잡다한 실시간 UI를 늘리지 않기 위함)
- **이벤트는 종류별로 별도 시스템을 만들지 않기로 함** — 밑단은 확률 굴림 하나(좋음/나쁨/강도)만
  있고, "외계인 발견!", "운석 접근" 같은 텍스트/아이콘은 그 위에 씌워지는 랜덤 플레이버일 뿐.
  이벤트 종류마다 대응 메커니즘을 따로 만들면 콘텐츠가 쌍으로 계속 불어나는 구조가 되어
  과거 "우주타이쿤" 방향에서 스코프가 커져 접었던 것과 같은 문제가 재발할 수 있다고 판단.
- **탐사 중 스토리 이벤트(`StoryEvent`)와는 별개 시스템** — 처음엔 기존 스토리 선택지 UI를
  재사용하려 했으나, 이벤트마다 서사 콘텐츠를 계속 써야 해서 스코프가 커진다고 판단해 폐기.
  보유 행성 이벤트 시스템은 순수 수치/UI 레이어로만 구현.

### 폐기된 중간 안들 (왜 접었는지 — 나중에 같은 논의 반복 방지용)
- **위험도 게이지 + "관리" 액션으로 낮추기**: 관리로 위험도를 언제든 예방/리셋할 수 있으면
  "방치하면 어떻게 되는가"에 대한 답이 없어지고, 매도라는 선택지가 설 자리가 사라짐
  (강화든 관리든 결국 다 해결되니 손해가 없음).
- **나쁜 이벤트 = 행성 "고장" 상태(생산 0, 수리 전까지 지속)**: 관리 액션을 없앤 뒤에도
  한동안 유지하려 했으나, "고치면 그만"이라 이것도 결국 매도를 무의미하게 만드는 동일한
  문제 재발 — 지속형 생산 배율 디버프(위 최종안)로 대체. 배율 디버프는 "고치는" 개념
  자체가 없고 매도로만 정리되므로 이 문제가 없음.
- **미수금(안 걷은 누적 방치수익) 일부를 이벤트로 훔치기**: 분당 생산량 표시값은 안 건드리고
  "쌓여있는데 아직 안 걷은 돈"만 건드리는 안. 앱을 켜두면 60초 주기로 자동 수집돼 미수금이
  거의 안 쌓이므로 사실상 오프라인일 때만 피해가 발생 — 화면에 표시된 분당 생산량과 실제
  수령액이 온라인/오프라인에 따라 달라지는 일관성 문제로 폐기.
- **생산 배율/시세 등락폭을 이벤트당 고정값(±20% 등)으로 처리**: 이벤트가 터지면 결과가
  항상 같은 크기라 "이벤트가 터졌다"는 사실 자체가 결과를 다 말해줘서 밋밋함 — 등락폭도
  매번 랜덤(위 최종안)으로 대체.
- **좋음/나쁨 확률을 타입별 `eventRate`(생성 시 랜덤) 그대로 사용**: 처음엔 은하가 우연히
  블랙홀급으로 나쁜 확률을 뽑는 걸 막으려고 타입별 범위 자체는 유지하는 게 맞다고 봤으나,
  실제 정의된 값을 확인해보니 무대기·소행성(COMMON, 저가) 같은 저티어가 90~97%라는
  극단적으로 좋은 확률을 받고, 오히려 블랙홀(RARE, 고가)이 50~70%로 더 나쁘게 나오는
  역전이 있었음. "위험도가 높다"를 "평균적으로 더 자주 진다"로 잘못 번역한 것 — 비싸고
  희귀한 자산이 오히려 나쁜 투자처가 되어버리는 문제라 폐기. 희귀도 기준 확률표(위
  최종안)로 대체.
- **"안전한 타입"을 risk 하나로 규정한 표현**: 심각한 사고 확률표를 만들 때 "무대기·소행성 =
  가장 안전"이라고 라벨을 붙였는데, 이건 risk(빈도) 축만 본 것이고 좋음/나쁨 확률(희귀도
  축)은 둘이 서로 다름(COMMON 45~55% vs UNCOMMON 40~48%)을 간과한 부정확한 표현이었음 —
  두 축은 완전히 독립적이라 하나로 뭉뚱그려 부르면 안 됨. 지적받고 수정.
- **나쁜 이벤트 중 일부만 별도 확률로 "심각한 사고"(생산 마이너스) 분기**: risk 기반으로
  "나쁜 이벤트 중 X%는 배율이 0 밑으로" 같은 별도 서브 확률 시스템을 만들려 했으나,
  "그냥 이벤트가 덮어쓰지 않고 누적되게 하면 불운한 스트릭 자체가 자연히 사고가 되는데
  굳이 별도 확률이 필요하냐"는 지적으로 폐기 — 덮어쓰기 대신 누적(+클램프) 방식(위
  최종안)으로 대체. 별도 확률 브랜치·전용 값 범위가 없어져 더 단순해짐.
- **생산 변동을 "%"로 표시**: 배율이 덮어쓰기일 때는 항상 −30%~+30% 안이라 괜찮았지만,
  누적 방식으로 바뀌면서 −100%를 넘나드는 값(예: −115%)이 나올 수 있게 됨 — 퍼센트로는
  버그처럼 보여서 폐기. 코인/시간 단위로 통일(위 최종안)하고, "시간 수익" 행 자체가
  부호에 따라 "생산 진행"/"생산 중단"으로 라벨이 바뀌는 방식으로 대체.

### 구현 체크리스트 (1차 구현 완료, 실기기 테스트 전)
- [x] `Planet`/`PlanetEntity`에 `productionMultiplier: Double = 1.0`, `marketAdjustment: Long = 0`,
      `lastEventTime: Long` 필드 추가, Room 마이그레이션(`MIGRATION_14_15`, DB v14→v15)
- [x] `PlanetMetaDataTable.kt`의 `eventRateMin/Max`를 타입별 값 대신 희귀도별 확률표
      (COMMON 45~55, UNCOMMON 40~48, RARE 35~42, EPIC 30~37, LEGENDARY 25~32)로 교체.
      `risk`(빈도) 관련 `riskMin/Max`는 기존 타입별 값 그대로 유지
- [x] `GameConstants`에 이벤트 간격 공식(`planetEventIntervalHours`), 이벤트 1회당 등락폭
      범위(`PLANET_EVENT_DELTA_MIN/MAX`), 누적치 바닥·천장(`PLANET_EVENT_MULTIPLIER_FLOOR
      = -0.30` / `CEILING = 3.0`), 시세 변동폭 비율(`PLANET_EVENT_MARKET_UPGRADE_INVESTMENT_RATIO`) 추가
- [x] `CollectProfitUseCase`에 `rollPlanetEventIfDue` 추가 — 마지막 이벤트 이후 간격 경과 시
      1회 롤, `eventRate`(희귀도 기준)로 좋음/나쁨 판정 → 등락폭(델타) 랜덤 롤 →
      `productionMultiplier`에 **누적**(이전 값 + 델타, 바닥·천장 클램프) 반영. `marketAdjustment`는
      매번 "지금 누적된 생산 배율" 기준으로 다시 계산(따로 안 누적, 항상 파생값). `lastEventTime` 갱신
- [x] `effectiveProduction`/`preciseProduction`에 `productionMultiplier` 곱하기 반영 — 배율이
      음수까지 갈 수 있어 두 값 다 마이너스가 될 수 있음(의도된 동작)
- [x] `Planet.marketValue` 확장 프로퍼티 신설(`buyPrice + upgradeInvestment + marketAdjustment`) —
      `SellPlanetUseCase`·`SyncLeaderboardUseCase`·`AssetViewModel`·`PlanetDetailScreen`·
      `PlanetScreen`(목록 정렬·매도 다이얼로그)·`ExploreScreen`(슬롯 부족 시 매도 플로우)의
      기존 `buyPrice + upgradeInvestment` 중복 계산을 전부 이걸로 교체해 일관성 확보
- [x] `PlanetDetailScreen`: "행성 가치" 행으로 매입가+강화투자액 통합(강화는 별도 투자금이
      아니라 행성 자체가 커지는 것이라는 피드백 반영), "시간 수익" 행이 부호에 따라
      "생산 진행"/"생산 중단"으로 라벨·색 전환(퍼센트 표시 행은 제거), "생산량" 행도
      마이너스면 빨강. `PlanetScreen` 목록 카드에 상태 색 점 추가, 매도 다이얼로그도
      "행성 가치"(매입가+강화투자액) + "시세 변동" 두 줄로 정리(더하면 정확히 매도가가 나오게)
- [x] 신규 구매 행성 초기값 확인 — `productionMultiplier`/`marketAdjustment`/`lastEventTime`
      모두 `Planet` 기본값(`GeneratePlanetsUseCase`가 명시적으로 안 넘겨도 1.0/0/now로 채워짐)
- [x] `./gradlew compileDebugKotlin` 통과 확인 (여러 차례 리팩터링 라운드 모두 빌드 확인)
- [x] **"소식" 탭 추가** — 실기기 테스트 중 "이벤트가 터졌는지 어떻게 알아?"라는 질문에서
      추가로 구현. `PlanetScreen`의 기존 "보유 행성"/"도감" 탭 옆에 세 번째 탭으로 추가.
      "일지"로 하려다 탐사 탭의 "탐사 일지"와 이름이 겹쳐 헷갈릴 수 있어 "소식"으로 확정
  - 신규 테이블 `planet_event_log_table`(`MIGRATION_15_16` 생성 → `MIGRATION_16_17`에서
    스키마 변경, DB v15→v17) — 이벤트가 실제 발생한 시점(오프라인 정산 포함)에
    `CollectProfitUseCase.rollPlanetEventIfDue`에서 같이 기록
  - 최근 24시간 이내 로그만 최신순으로 표시(`GetPlanetEventLogsUseCase`), 오래된 로그는
    기존 `MAX_EVENT_LOG_COUNT`(200) 상수로 자동 정리
  - 플레이버 텍스트 풀(`PlanetEventFlavor.kt`) — 좋음/나쁨 각 24개("외계 문명과 우호적 접촉",
    "소행성 근접 경보" 등)를 매 이벤트마다 랜덤으로 뽑아 사용, 같은 문구 반복 방지
  - 로그 항목은 `productionDeltaPerHour`(이번 이벤트 하나만의 시간당 코인 기여분)로 표시 —
    상세화면의 누적치와 역할이 다름(로그=이번 사건, 상세화면=지금까지 총합)
- [x] **도감 탭 레이아웃 개선** — variant가 최대 40개인 타입도 있어 세로 2열 그리드가 끝없이
      길어지는 문제 지적받아, `LazyHorizontalGrid(rows = GridCells.Fixed(2))`로 교체.
      등급 섹션마다 세로 높이는 항상 2줄 고정, 나머지는 가로 스크롤. 카드 내용 세로 중앙
      정렬로 위아래 여백 불균형도 같이 수정
- [x] 실기기에서 이벤트 발생·생산 진행/중단·시세·소식 로그 반영 실제 확인 (2026-09-02).
      임시 오버라이드(간격 0.01h, delta 0.30, `isBad=true`)로 **생산 배율이 마이너스까지
      내려가는 케이스** 만들어 "생산 중단" 표시·마이너스 정산·손실 다이얼로그까지 확인 후
      전부 원복. `git diff`에 `GameConstants`/`RollPlanetEventUseCase` 안 나오는 것으로 원복 검증됨

### 마이너스 순수익이 실제 코인 손실로 이어지지 않던 버그 발견·수정 (2026-08-25)

"생산 중단 상태 행성이 쌓이면 광고로 2배 받기 팝업은 어떻게 되나?"라는 질문에서 시작해 코드를
따라가보니, **행성들의 순수익 합계가 마이너스여도 실제 코인 잔액은 전혀 안 깎이는 버그**를
발견함:
- `CollectProfitUseCase`가 `if (totalEarned > 0) userRepository.addCoins(...)`로 감싸져
  있어서, 총합이 마이너스면 각 행성의 `totalProfit` 필드엔 마이너스가 기록되면서도(장부상으로만)
  실제 코인은 그대로였음
- `AppSessionViewModel.checkPendingProfit()`도 `pending.coins > 0`일 때만 분기가 있어서,
  마이너스면 다이얼로그도 안 뜨고 수집(collect) 자체가 호출도 안 됐음
- 결론: TODO 상단 "방치만 해도 손해 볼 수 있나?" 항목이 완료로 적혀있었지만, 실제로는
  `productionMultiplier`가 마이너스로 가는 것까지만 되고 그게 코인 차감으로 이어지는
  마지막 단계가 빠져있었음 — **수정 완료**

수정 내용:
- `CollectProfitUseCase`: 순수익이 마이너스면 `userRepository.deductCoinsClamped()`로
  바닥(0) 클램프 차감하도록 분기 추가 (`addCoinsAtomic`엔 바닥 클램프가 없어서 그냥
  마이너스를 더하면 잔액이 음수까지 갈 수 있어 기존 클램프 함수 재사용)
- `AppSessionViewModel.checkPendingProfit()`: 손해도 이득과 대칭으로 처리 — 10분
  (`OFFLINE_PROFIT_DIALOG_THRESHOLD_MINUTES`, 이득과 공유하는 상수) 이상 쌓였으면 알림
  다이얼로그, 짧으면 조용히 자동 반영
- 신규 `OfflineLossDialog.kt` — "생산이 중단된 행성 때문에 N코인을 잃었어요" + 확인 버튼
  하나(광고로 손해를 2배로 만드는 옵션은 없음). `MainScreen`에서 `pending.coins` 부호로
  기존 `OfflineProfitDialog`와 분기

**다음 세션에 이어서 할 것:**
- [x] 실기기에서 손실 다이얼로그 실제로 뜨는지, 코인이 진짜 깎이는지 확인 (2026-09-02 완료 —
      다이얼로그 정상 노출 + 코인 바닥 클램프 차감 확인)
- [x] `OfflineLossDialog` 디자인을 `OfflineProfitDialog`와 정렬 (2026-08-27): 확인 버튼을
      솔리드 빨강 → `SpaceLight` 중립 채움 + `ButtonDepth` 입체감으로, 설명문은
      `TextSecondary`/`bodyMedium`로 내리고 금액만 `-N 코인` 짧게 빨강 headline. 빨강이
      보더+headline+버튼 3군데 깔려 "너무 크고 빨갛다"던 피드백 해소
- [ ] `OFFLINE_PROFIT_DIALOG_THRESHOLD_MINUTES`(10분)를 이득/손실 다이얼로그가 같이 쓰는데,
      "10분은 손실 쪽엔 좀 짧은 것 같다"는 의견 있었음 — 일단 10분으로 테스트 먼저 하고
      나중에 필요하면 손실 전용 상수로 분리 검토
- [x] 마이너스 재현용 테스트 오버라이드 되돌림 (2026-08-27, 커밋 전):
      - `GameConstants.planetEventIntervalHours` → `6.0 - (risk - 2) * 0.0306`
      - `GameConstants.PLANET_EVENT_DELTA_MIN/MAX` → `0.03`/`0.30`
      - `GameConstants.PLANET_EVENT_NOTIFY_DELTA_THRESHOLD` → `0.25`
      - `RollPlanetEventUseCase`의 `isBad` → `Random.nextInt(100) < planet.eventRate`

### 매도가가 마이너스로 가던 버그 + 매도 다이얼로그·강화 안내 정리 (2026-09-03)

폭락 행성 매도 시 수수료가 `--46 코인`처럼 이중부호로 뜨던 걸 추적하다, `marketValue`
(= 매입가+강화액+시세변동)가 시세 폭락 시 **음수까지 가서 매도 시 코인을 오히려 뺏기던**
버그를 발견. 유저와 논의해 최종안 = **"악재로 생산량·시세 둘 다 내려가되(커플링 유지),
매도가는 0에서 바닥"** (주식처럼 산 값보다 싸게 팔 순 있어도 파는데 돈을 내진 않음).

- `RollPlanetEventUseCase`: `marketAdjustment`를 `-(매입가+강화액)`에서 clamp → `marketValue`
  가 절대 음수 안 됨. 소식 로그의 `eventMarketDelta`도 클램프 후 실제 변화분으로
- `Planet.marketValue`: 방어적으로 `.coerceAtLeast(0L)` (예전 테스트 데이터 대비)
- `SellPlanetUseCase`: 원래 로직 그대로 (proceeds ≥ 0 보장됨)
- 매도 다이얼로그: `행성 가치` → `투자액`(매입가+강화액), `시세 변동`에 `(±N%)` 병기
  (% = 투자액 대비, 정수 대략치), 수수료 0이면 줄 숨김
- `PlanetDetailScreen` 투자 현황 카드: `행성 가치`/`매도 예상가`(5% 차이라 중복) →
  `투자액` / `현재 매도가`(투자액 대비 초록·빨강). 죽은 `profitRate` 변수 제거
- **강화 다이얼로그에 효과 미리보기 추가** — "분당 생산량 N → M" + "레벨이 오르면 생산량·
  드롭량이 늘고 강화 코인은 매도가에 더해져요" 한 줄. 저렙에서도 이득이 눈에 보이게
- 분당 드롭 자원 수치 폰트 `bodySmall`(픽셀) → `NumericSmall`(다른 수치와 통일)
- **ⓘ 설명문 전체 톤 통일** — 해요체·짧게·군더더기 제거(탐사 안내 톤 기준).
  `PlanetDetailScreen` StatsInfoDialog / `InfoDialog.kt`(우주선·우주인) / `ExploreScreen`
  탐사 안내 / `AssetScreen` 자산 항목. 토스트·에러·빈 화면 문구는 상태 메시지라 제외.
  스플래시·로그인 안내 문구는 유저가 직접 편집

## 게임 디테일/연출 (1차 마무리, 커밋 대기)

기능 점검은 어느 정도 마무리했다고 보고, 이제부터는 "완성도" 체감을 높이는 연출·디테일
작업 우선. 사운드는 에셋이 없어서 당장은 어려움 — 촉각(진동)으로 일부 대체.

### 완료
- 행성 강화: 결과 즉시 표시 → 충전(Charging) 연출 후 결과 공개(Revealing)로 변경.
  안전구간 0.9초/위험구간 1.8초, 위험구간은 더 빠르고 붉게 펄스
- 결과 공개를 성공/실패/위험구간 하락 3단계로 차등 연출(스프링 확대/절제된 흔들림/
  강한 흔들림) + 결과별 진동 패턴(`util/Haptics.kt`, `VIBRATE` 권한 추가)
- 강화 결과가 타이머로 자동으로 안 사라지고 "확인" 눌러야 사라지도록 변경
- 강화 다이얼로그를 기본 AlertDialog → 테두리+텍스처 커스텀 Dialog로 교체,
  테두리 색이 위험구간·결과에 따라 반응
- `PlanetDetailScreen` 레이아웃 여백 버그 수정, 강화하기/매도 버튼을 한 줄에
  비율 배치(매도 32% 왼쪽·아웃라인, 강화하기 68% 오른쪽·주 액션)
- 강화 말고 다른 화면(탐사/영입/연구소)도 훑어봄 — 탐사가 유일하게 확률·희귀도 롤이 있는
  "발견" 요소인데도 결과가 즉시 통째로 표시돼 긴장감이 없는 게 가장 시급한 격차로 확인됨
  (영입은 스탯을 미리 보고 사는 구조라 뽑기 긴장감 자체가 설계상 낮음, 연구소는 업그레이드가
  확정적이라 연출 필요성이 낮음 — 둘 다 우선순위 낮게 보류)
- 탐사 결과 다이얼로그에 희귀도 기반 리빌 연출 추가: 발견한 행성이 있으면 결과를 즉시 안 보여주고
  "미확인 신호 분석 중…" 스캔 연출(희귀도가 높을수록 더 오래 끔, COMMON 0.4초~LEGENDARY 1.8초) →
  스프링 확대 리빌(테두리·배지를 희귀도 색으로) → 희귀도별 강도 차등 진동
  (`util/Haptics.kt`의 `vibrateDiscoveryReveal`). 구매 버튼은 리빌 전엔 숨김.
  `PlanetScreen.kt`에 중복 정의돼 있던 희귀도 색상/라벨 맵은
  `presentation/component/RarityStyle.kt`로 공용화

### 다음에 이어서 할 것
- [x] 탐사 리빌 연출 실기기 테스트 완료 (테스트용 임시 수치는 되돌려놓음)
- [x] 행성 강화 연출 실기기 테스트 완료
- [x] 연구소 화면 상단 결과 메시지 배너 완전 제거 — 배너가 스크롤 컨테이너 안에 있어 아래쪽
      카드에서 업그레이드하면 못 보고 사라지는 문제를 고치려다, 애초에 카드 자체가 이미
      실시간으로 다 보여준다는 걸 확인(레벨 뱃지 즉시 갱신, 부족하면 버튼 비활성화+비용 빨간색)
      — 배너/메시지 상태를 스크린·뷰모델에서 통째로 삭제(`ResearchLabScreen.kt`,
      `ResearchLabViewModel.kt`). 연타 충돌(Conflict, 비용 자동 환불됨)은 조용히 무시,
      재시도하면 정상 처리됨. 영입/격납고 화면은 배너가 원래 항상 고정 노출이라 문제 없어 안 건드림.
      실기기 테스트 완료
- [x] (검토 후 보류) 탐사 목록 화면 대기시간 카운트다운/프로그레스바 — 완료 시 알림(`ExplorationCompleteWorker`)이
      이미 있어 실시간 표시 필요성이 낮고, 앱 전체에 애니메이션이 잡다하게 깔리는 걸 피하고 싶다는
      판단으로 안 하기로 함. 애니메이션은 강화/탐사 리빌처럼 "확률 롤의 결정적 순간"에만 쓰는 걸 원칙으로
- [x] (검토 후 유지) 매도를 디테일 화면에서 뺄지 고민했었음 — 목록 화면에도 이미 매도가 있어서
      중복이지만, "디테일 보다가 팔기로 결정" 흐름의 편의성 때문에 일단 유지하기로 결정

## 기능·로직 점검 및 수정 (완료, 8개 커밋)

여러 라운드에 걸쳐 게임 로직 전반을 점검하고 발견된 버그를 수정했다. 출시 준비(광고·서명·배포)는
기능·디자인이 마무리된 뒤로 미루고, 이번엔 기능적 정확성에만 집중했다.

### 경제 로직 — 코인/자원 이중 차감 (e49b612, 06a2c46, 3524f70)
- 강화(행성·우주선·연구소)·훈련·영입·구매 각 UseCase가 "잔액 확인 후 별도로 차감"하던 방식(TOCTOU)을
  전부 원자적 `deductCoins`/`consume`으로 교체 — 연타 시 이중 차감·음수 잔액 가능성 제거
- `ResourceRepository.consume/add`를 DB 레벨 원자 쿼리로 교체 (자원 수량 음수화·동시 지급 유실 방지)
- 우주선·연구소 강화는 코인/자원은 원자적으로 막았지만 등급·레벨 반영 자체는 안 막혀 있어서,
  연타 시 비용은 두 번 나가고 등급은 한 단계만 오르는 문제가 남아있었음 — `expectedGrade`/
  `expectedLevel` DB 가드 추가, 충돌 시 비용 환불 + `Conflict` 결과 반환
- 행성 매도(`SellPlanetUseCase`)도 동일 패턴으로 연타 시 코인 중복 지급되던 것 수정
- 행성 구매·슬롯풀 코인 전환도 `ExploreViewModel`에 재진입 방지 플래그(`isResolvingDiscovery`) 추가

### 상태 동기화·타이밍 (e49b612, a094b68, 06a2c46, 3524f70)
- 강화 실패 후 "광고 보고 되돌리기" 버튼이 토스트 타이머(3초)에 묶여 다이얼로그가 열려있어도
  사라지던 것 — 새 강화 시도 시에만 무효화되도록 분리
- `ChooseStoryEventUseCase`: 코인 부족으로 페널티가 실제로 안 깎였는데도 메시지엔 항상
  "-N코인"이 뜨던 것 — `deductCoinsClamped`로 실제 차감액만 표시
- 스토리 이벤트 선택 자체도 DB 가드가 없어 연타 시 보상/페널티가 중복 적용될 수 있던 것 —
  `claimEventChoice`로 먼저 원자적 확정 후에만 보상 적용
- 리더보드가 구매 시점에 고정된 채 갱신 안 되는 `Planet.currentValue`(죽은 필드)를 써서
  강화 투자액이 순위 계산에서 누락되던 것 — `buyPrice + upgradeInvestment`로 통일
- 훈련 완료가 포그라운드 폴링·백그라운드 워커 양쪽에서 동시에 처리될 수 있어 숙련도 계산식이
  바뀌면 이중 적용될 여지가 있던 것 — `status='TRAINING'` DB 가드 추가
- 스킵대기 광고 시청 중 훈련이 이미 끝났는데 광고 콜백이 무조건 TRAINING으로 되살리던 것 —
  전용 `extendTraining`(가드 포함) 메서드로 교체

### 방치 수익 계산 (d80ff90)
- 60초 방치수익 루프와 앱 복귀 시 자동 수집이 거의 동시에 실행되면 같은 경과 구간이
  중복 지급될 수 있던 것 — `CollectProfitUseCase`에 Singleton Mutex + 락 안 DB 재조회로 수정
- 정산에 쓰던 `effectiveProduction`이 분당 정수 표시용으로 미리 내림된 값이라 경과 시간에
  곱하면 소수점 손실이 누적돼 최대 약 7% 덜 지급되던 것 — 내림 안 하는 `preciseProduction` 추가
- `AssetScreen`의 실시간 코인 표시가 액션 직후 순간적으로 어긋날 수 있던 중복 로직을
  `rememberLiveCoinDisplay` 재사용으로 정리
- 탐사 자원 보상이 티어와 무관해 고티어가 저티어보다 시간당 효율이 크게 떨어지던 것 —
  코인 보상과 같은 방식(소요 시간 비례 + 티어당 완만한 배율)으로 스케일링

### UI/네비게이션 (904af42)
- 딥링크로 특정 화면에 진입한 뒤 기기를 회전하면 Activity가 재생성되며 같은 딥링크
  내비게이션이 재실행되던 것 — 읽은 즉시 Intent에서 제거(consume)하도록 수정
- 탭 딥링크(explore/planet/hq/asset)가 하단 탭 바와 달리 `popUpTo`/`restoreState` 없이
  백스택에 중복 쌓이던 것, 서브 화면(탐사기록/행성상세/랭킹/HQ 3분야) 진입에 연타 가드
  (`launchSingleTop`)가 없던 것 수정

### WorkManager (3524f70)
- `TrainingCompleteWorker`·`ExplorationCompleteWorker`·`PlanetMaintenanceWorker`에
  예외 발생 시 `Result.retry()` 추가 (이전엔 일시적 DB 오류로 조용히 영구 실패해 해당
  워커가 담당하는 알림이 다시는 안 뜰 수 있었음)
- 초기화·취소/재예약·빈 리스트 엣지케이스는 이미 정상이었음을 확인

### 인증/랭킹 (46ed63b)
- 구글 로그인 취소 시 "취소는 무시"라는 주석과 달리 실제로는 매번 에러 문구가 뜨던 것 —
  `GetCredentialCancellationException` 별도 처리 추가
- `GoogleIdTokenCredential.createFrom()`이 던질 수 있는 `GetCredentialException`이 아닌
  예외(파싱 오류 등)를 못 잡아 크래시 가능하던 경로에 포괄 catch 추가
- 랭킹 화면이 상위 50위 밖 유저에게 항상 "51위"라는 실제와 무관한 숫자를 보여주던 것 —
  집계 쿼리가 없어 정확한 순위를 알 수 없으므로 "50위 밖"으로 정직하게 표시

### 확인만 하고 손 안 댄 것
- `ExploreViewModel.dispatch()`가 탐사를 DB에 먼저 저장하고 워커를 나중에 예약해 그 사이
  프로세스가 죽으면 워커 없이 남을 수 있음 — 5초 주기 포그라운드 폴링이 자가 복구, 영향은
  알림이 최대 5초 늦는 정도라 손대지 않음
- `AuthViewModel.signOut()`이 로컬 게임 데이터(코인·행성)를 안 지우는 죽은 코드 — 현재
  아무도 호출 안 해서 활성 버그는 아니지만, 나중에 로그아웃 UI를 추가할 때 지뢰가 될 수 있음
  (→ 2026-09-03 해결, 위 "클라우드 세이브 Phase 4" 참고)
- `doge://feed` 딥링크가 매니페스트 주석·명세 문서에만 있고 실제로 아무도 안 보내는 죽은 참조 —
  확인만 하고 미수정
- `AssetScreen`의 판매 다이얼로그가 열려있는 동안 자원 수량이 바뀌어도 다이얼로그 표시값이
  갱신 안 되는 경우가 있으나, 백엔드가 이미 원자적 `consume()`으로 보호돼 있어 실질 위험 없음

## 이모지 → 커스텀 아이콘 교체 (완료, 03fb8af 커밋·푸시됨)

연구소 4분야(🔭🌌👥🚀), 탐사 카테고리/우주인 전문분야(자원 아이콘 재사용), 잠금/코인/에너지/광고(🔒💰🪙⚡📺), 행성/트로피/메달(🪐🏆🥇🥈🥉), 도감 등록/신규(📖🆕), 진행중 탐사/미확인 보고서(📡📬), 팀 인원(👥), 강화 위험구간 경고(`PlanetDetailScreen.kt`), 자산 통계(📊📈), 선택 체크마크(✓), 홈 버튼(🏠), 강화/탐사 결과 토스트(🎉💥😞🔄✨) — 아이콘 19종 추가 완료.

추가로 이번 라운드에서 정리:
- 시스템 알림 타이틀 3곳(훈련 완료·행성 수익·탐사 완료)의 장식용 이모지 제거, `TrainingCompleteWorker`의 알림 아이콘이 딥링크 대상(HQ)과 안 맞던 것도 수정
- `ChooseStoryEventUseCase.kt`의 스토리 로그 문자열 "⚠" 제거 → `ExpeditionHistoryScreen`에서 `ic_ui_danger` 아이콘으로 렌더링, 위험/경고 아이콘을 `ic_ui_danger`로 통일(`ic_ui_warning` 삭제)
- `ExpeditionCategory`·`AstronautSpecialty`·`ResearchField`의 미사용 이모지 필드, `AssetScreen`의 `ResourceRow` 미사용 이모지 폴백 분기 등 죽은 코드 정리
- 상태 표시 점(🟡🟢)은 이모지 없이 `StatusGreen`/`StatusRed` 색상으로 처리되어 있음을 확인, 별도 작업 불필요

남은 이모지 없음(Compose UI, 알림, 로그 문자열, 리소스 전체 재확인 완료).

## 출시 전 확인 필요

기능·디자인이 마무리된 뒤 진행. 아래 4개는 재점검 결과 이미 로컬 설정 완료된 상태(문서만 안 갱신돼 있었음):

- [x] AdMob release 빌드 유닛 ID — `local.properties`에 실제 ID 설정 확인됨 (`ADMOB_APP_ID` 등)
- [x] PF Stardust ExtraBold 폰트 파일 — `res/font/pf_stardust_extrabold.ttf` 존재 확인
- [x] `google-services.json` — `app/`에 존재 확인
- [x] `local.properties`에 `FIREBASE_STORAGE_BASE_URL` 실값 — 설정 확인됨
- [ ] Cloud Functions 배포 여부 미확인 (`functions/` → `npm install && firebase deploy --only functions`) — 로컬에 firebase CLI가 없어 배포 이력을 직접 확인 못 함, Firebase 콘솔에서 확인 필요
- [ ] Play Store 출시용 서명 설정(keystore + `signingConfigs`) — release 빌드에 아직 없음
- [ ] release `isMinifyEnabled` — 현재 `false`, R8/난독화 적용 여부 결정 필요
- [ ] 사운드 에셋 확보 및 적용 — 강화/탐사 결과 등 현재 진동으로 대체 중인 곳에 효과음 추가 검토
      (게임 디테일/연출 작업 중 보류했던 항목, 에셋 준비되는 시점에 맞춰 진행)
